import Portrait from './../Media/home-image.jpg';

const Hero = () => {
    return (
      <div className="hero">
        <img className='portrait' src={Portrait} />
      </div>
    );
  };
  
  export default Hero;